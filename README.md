# Soul Online alpha 1.00 source patch

Apply over the existing Soul Online project root, then run:

```bash
npm install
npm run build
```

This patch focuses on the hunting-field UI rebuild, art-grade field HUD assets, mobile safe-area layout, and field performance tuning.
