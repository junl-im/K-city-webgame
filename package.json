{
  "name": "soul-online-alpha",
  "version": "1.36.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "npm run preflight && node scripts/cleanupLegacySvg.mjs && tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "preflight": "node scripts/verifyProjectIntegrity.mjs"
  },
  "dependencies": {
    "firebase": "12.13.0",
    "pixi.js": "8.13.1"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "8.0.16"
  }
}
